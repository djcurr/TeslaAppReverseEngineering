package net.time4j.format;

import java.util.Locale;

/* loaded from: classes5.dex */
public interface x {
    String B(Locale locale, v vVar, n nVar);

    String C(Locale locale, boolean z11, n nVar);

    String c(Locale locale);

    String d(Locale locale, boolean z11, n nVar);

    String e(Locale locale, v vVar, n nVar);

    String f(Locale locale, boolean z11, n nVar);

    String g(Locale locale, v vVar, n nVar);

    String i(Locale locale, boolean z11, n nVar);

    String k(Locale locale, v vVar, n nVar);

    String l(Locale locale, v vVar, n nVar);

    String m(Locale locale, v vVar, n nVar);

    String n(Locale locale, v vVar, n nVar);

    String p(Locale locale, v vVar, n nVar);

    String r(Locale locale, boolean z11, n nVar);

    String s(Locale locale, v vVar, n nVar);

    String t(Locale locale, v vVar, int i11);

    String u(Locale locale, boolean z11, n nVar);

    String v(Locale locale, v vVar, n nVar);

    String z(Locale locale, boolean z11, n nVar);
}